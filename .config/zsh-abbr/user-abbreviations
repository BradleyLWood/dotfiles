abbr "ga"="git add"
abbr "gaa"="git add --all ."
abbr "gb"="git branch"
abbr "gba"="git branch -a"
abbr "gbd"="git branch -D"
abbr "gc"="git commit"
abbr "gca"="git commit -a"
abbr "gcn"="git commit -m '$(date +"%Y-%m-%d %H:%M:%S")'"
abbr "gco"="git checkout"
abbr "gcob"="git checkout -b"
abbr "gd"="git difftool"
abbr "gl"="git log --pretty=format:\"%Cgreen%h%Creset - %Cblue%an%Creset @ %ar : %s\""
abbr "gl2"="git log --pretty='format:%Cgreen%h%Creset %an - %s' --graph"
abbr "glv"="git log --stat"
abbr "gm"="git merge --no-ff"
abbr "gp"="git push"
abbr "gpt"="git push --tags"
abbr "gpr"="git pull --rebase"
abbr "gpom"="git pull origin master"
abbr "grh"="git reset --hard"
abbr "gs"="git status"
abbr "cga"="config add"
abbr "cgs"="config status"
abbr "cgc"="config commit"
abbr "p"="sudo pacman"
abbr "pgu"="sudo -u postgres"
abbr "v"="nvim"
abbr "vs"="sudo nvim"
abbr "ta"="tmux attach -t"
abbr "tl"="tmux list-sessions"
abbr "ts"="tmux-sessionizer"
abbr "tk"="tmux kill-session -t"
abbr "h"="herdr"
abbr "bws"="export BW_SESSION=$(bw unlock --raw)"
